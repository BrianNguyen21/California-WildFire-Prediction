import { Typography } from '@mui/material';
import { ReactElement } from 'react';

export const TitleBar = (): ReactElement => {
  return <Typography variant="h4">California Wildfire Prediction</Typography>;
};
